class Transcriber:
    def __init__(self, config):
        self.algo = config['services']['transcriber']['alogrithm']
        self.algo_config = config['services']['transcriber']['config']

        if self.algo
    def transcribe(self, text):
        