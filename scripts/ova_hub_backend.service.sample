[Unit]
Description=openvoiceassistant hub backend
After=multi-user.target

[Service]
WorkingDirectory=OVAPATH
ExecStart=/bin/bash OVAPATH/scripts/start_backend.sh
User=USER

[Install]
WantedBy=multi-user.target